# COMMON_ISSUES

Terse, tool-specific notes the agent consults at startup (truncated in the system prompt if this file grows).

- Append new bullets only when a failure pattern is confirmed reusable (e.g. after failure-analyzer at high confidence).
- Do not paste secrets, keys, or raw API payloads.

<!-- Example (remove or replace with real entries):
- **http_request**: If Tokenaru returns 429, reduce concurrency and retry after a short delay.
-->
